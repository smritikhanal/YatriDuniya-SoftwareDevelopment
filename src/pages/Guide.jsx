export default function Guide({ onBack }) {
    return (
      <div>
        <h2>Package Detailseeee</h2>
        <p>This is the package content displayed without leaving the page.</p>
        <button onClick={onBack}>Go Back</button>
      </div>
    );
  }
  